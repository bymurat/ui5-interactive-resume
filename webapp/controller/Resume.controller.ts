import BaseController from "./BaseController";

/**
 * @namespace ui5.interactive.resume.controller
 */
export default class Resume extends BaseController {
	public onInit(): void {
		// placeholder — section handlers will be wired up in later milestones
	}
}
