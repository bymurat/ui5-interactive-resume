<mvc:View
	controllerName="ui5.interactive.resume.controller.Timeline"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:rgc="ui5.interactive.resume.control"
	core:require="{
		formatter: 'ui5/interactive/resume/model/formatter'
	}"
	xmlns:core="sap.ui.core">

	<Page
		id="timelinePage"
		title="{i18n>sectionTimeline}"
		showNavButton="true"
		navButtonPress=".onNavBack">

		<content>
			<Panel
				class="sapUiResponsiveMargin"
				width="auto"
				backgroundDesign="Solid">
				<headerToolbar>
					<Toolbar>
						<Title
							text="{resume>/profile/firstName} {resume>/profile/lastName} {i18n>timelinePageTitleSuffix}"
							level="H2" />
						<ToolbarSpacer />
						<Select
							selectedKey="{path: 'i18n>langSwitcherLabel', formatter: '.getCurrentLanguageKey'}"
							autoAdjustWidth="true"
							change=".onLanguageChange"
							tooltip="{i18n>langSwitcherLabel}">
							<core:Item key="en" text="{i18n>langEnglish}" />
							<core:Item key="fr" text="{i18n>langFrench}" />
							<core:Item key="de" text="{i18n>langGerman}" />
							<core:Item key="lb" text="{i18n>langLuxembourgish}" />
						</Select>
					</Toolbar>
				</headerToolbar>
				<content>
					<rgc:ResumeGantt
						id="resumeGantt"
						rowHeight="48"
						class="sapUiContentPadding"
						employers="{resume>/timeline/employers}"
						phasePress=".onPhasePress"
						i18nTexts="{
							parts: [
								{ path: 'i18n>ganttAriaLabel' },
								{ path: 'i18n>ganttEmptyState' },
								{ path: 'i18n>ganttLegendParallel' },
								{ path: 'i18n>hatSapui5' },
								{ path: 'i18n>hatFullstack' },
								{ path: 'i18n>hatMobile' },
								{ path: 'i18n>ganttPresent' },
								{ path: 'i18n>ganttDurationYears' },
								{ path: 'i18n>ganttDurationMonths' },
								{ path: 'i18n>ganttDurationYearsMonths' },
								{ path: 'i18n>ganttAriaOnTeamAt' },
								{ path: 'i18n>ganttAriaPrimaryRole' },
								{ path: 'i18n>ganttAriaAlso' },
								{ path: 'i18n>ganttAriaParallel' }
							],
							formatter: 'formatter.packGanttTexts'
						}">
						<rgc:employers>
							<rgc:ResumeGanttEmployer
								employerId="{resume>id}"
								name="{resume>name}"
								kind="{resume>kind}"
								country="{resume>country}"
								countryEmoji="{resume>countryEmoji}"
								city="{resume>city}"
								start="{resume>start}"
								end="{resume>end}"
								current="{resume>current}"
								engagements="{resume>engagements}">
								<rgc:engagements>
									<rgc:ResumeGanttEngagement
										engagementId="{resume>id}"
										client="{resume>client}"
										department="{resume>department}"
										city="{resume>city}"
										clientLogoIcon="{resume>clientLogoIcon}"
										start="{resume>start}"
										end="{resume>end}"
										current="{resume>current}"
										parallel="{resume>parallel}"
										phases="{resume>phases}">
										<rgc:phases>
											<rgc:ResumeGanttPhase
												phaseId="{resume>id}"
												team="{resume>team}"
												role="{resume>role}"
												hat="{resume>hat}"
												additionalHats="{resume>additionalHats}"
												start="{resume>start}"
												end="{resume>end}"
												current="{resume>current}"
												summary="{resume>summary}"
												achievements="{resume>achievements}"
												technologies="{resume>technologies}" />
										</rgc:phases>
									</rgc:ResumeGanttEngagement>
								</rgc:engagements>
							</rgc:ResumeGanttEmployer>
						</rgc:employers>
					</rgc:ResumeGantt>
				</content>
			</Panel>
		</content>
	</Page>

</mvc:View>
